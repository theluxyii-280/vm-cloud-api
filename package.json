{
  "name": "vm-cloud-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "author": "only luxyii",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.68.0"
  }
}
